/// Integration test entry point.
///
/// Tests are organized under `tests/unit/` — each module covers a
/// different data-layer component (queries, services, etc.).
///
/// Common test helpers (e.g. in-memory DB setup) live in `tests/common/`.
mod common;
mod unit;